[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — prerender olmayan rotalar için (örn. /quiz/:category)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
